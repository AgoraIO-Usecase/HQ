function encrypt(algorithm, text, key) {
    return text;
}

function decrypt(algorithm, text, key) {
    return text;
}

module.exports = {
    encrypt: encrypt,
    decrypt: decrypt,
    supported: []
}